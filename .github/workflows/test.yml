name: GitHub Actions Demo
run-name: testing out GitHub Actions

on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: self-hosted
    steps:
      - name: test
        run: |
          A: $(python3 main.py)
          echo "A:${A}" >> $GITHUB_ENV


